"""Reads spec, plans steps for the investigation."""

from typing import Any


def plan_from_spec(spec: dict[str, Any]) -> list[dict]:
    """Parse spec and return ordered plan (steps). Stub."""
    raise NotImplementedError("Extract steps from spec; optionally use LLM to refine.")
