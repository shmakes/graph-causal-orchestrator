"""LangGraph: multi-agent graph for churn investigation."""

from typing import Any


def build_multi_agent_graph(config: dict[str, Any] | None = None):
    """Build a LangGraph with planner, data, causal, solution, reviewer agents. Stub."""
    raise NotImplementedError(
        "Add planner -> data -> causal -> solution -> reviewer nodes and edges; compile()."
    )
