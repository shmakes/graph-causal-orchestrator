"""LangGraph: single agent loop for churn investigation."""

from typing import Any


def build_single_loop_graph(config: dict[str, Any] | None = None):
    """Build a LangGraph StateGraph with one agent in a loop. Stub."""
    raise NotImplementedError("Use langgraph.StateGraph, add node, add conditional edge, compile().")
